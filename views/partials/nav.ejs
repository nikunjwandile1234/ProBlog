<nav class="navbar navbar-expand-lg sticky-top">
  <div class="container">

    <!-- Brand -->
    <a class="navbar-brand fw-bold d-flex align-items-center gap-2" href="/">
      ⚡ <span>ProBlog</span>
    </a>

    <!-- Search -->
    <form action="/search" class="d-none d-md-flex me-3">
      <input 
        name="q" 
        class="form-control form-control-sm" 
        placeholder="Search articles..." 
        style="min-width:260px"
      />
    </form>

    <!-- Right actions -->
    <div class="d-flex align-items-center gap-3">

      <!-- Theme toggle -->
      <button id="themeToggle" class="btn btn-sm btn-outline-secondary" type="button" title="Toggle theme">
        <i class="bi bi-sun"></i>
      </button>

      <ul class="navbar-nav d-flex flex-row align-items-center gap-3 mb-0">

        <% if (locals.user) { %>

          <% if (locals.user.role === "ADMIN") { %>
            <li class="nav-item">
              <a class="nav-link" href="/admin">Admin</a>
            </li>
          <% } %>

          <li class="nav-item">
            <a class="nav-link" href="/blog/add">Write</a>
          </li>

          <li class="nav-item">
            <a class="nav-link" href="/profile/<%= locals.user._id %>">Profile</a>
          </li>

          <li class="nav-item">
            <a class="btn btn-outline-danger btn-sm" href="/user/logout">Logout</a>
          </li>

        <% } else { %>

          <li class="nav-item">
            <a class="nav-link" href="/user/signin">Sign In</a>
          </li>

          <li class="nav-item">
            <a class="btn btn-accent btn-sm" href="/user/signup">Get Started</a>
          </li>

        <% } %>

      </ul>
    </div>

  </div>
</nav>


