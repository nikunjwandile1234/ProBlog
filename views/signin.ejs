<!DOCTYPE html>
<html lang="en">
<head>
  <%- include('./partials/head') %>
  <title>Sign In</title>
</head>
<body>

<%- include('./partials/nav') %>

<div class="container mt-5 mb-5" style="max-width:420px">

  <div class="card p-4">

    <h3 class="mb-2 text-center">Welcome back 👋</h3>
    <p class="text-muted text-center mb-4">Sign in to continue to ProBlog</p>

    <% if (typeof error !== "undefined" && error) { %>
      <div class="alert alert-danger"><%= error %></div>
    <% } %>

    <form method="POST" action="/user/signin">

      <div class="mb-3">
        <label class="form-label">Email</label>
        <input 
          name="email" 
          type="email"
          class="form-control" 
          placeholder="you@example.com"
          required
        >
      </div>

      <div class="mb-3">
        <label class="form-label">Password</label>
        <input 
          type="password" 
          name="password" 
          class="form-control" 
          placeholder="••••••••"
          required
        >
      </div>

      <button class="btn btn-accent w-100 mb-3">Sign In</button>

    </form>

    <div class="text-center mt-3">
      <span class="text-muted">Don't have an account?</span>
      <a href="/user/signup" class="fw-semibold">Create one</a>
    </div>

  </div>

</div>

<%- include('./partials/script') %>
</body>
</html>


