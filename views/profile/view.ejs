<!DOCTYPE html>
<html>
<head>
  <%- include('../partials/head') %>
</head>
<body>

<%- include('../partials/nav') %>

<div class="container mt-4">

  <!-- Profile Header -->
  <div class="card p-4 mb-4 text-center">
    <h3 class="mb-1"><%= profileUser.name %></h3>
    <p class="text-muted mb-0"><%= profileUser.email %></p>
  </div>

  <!-- Blogs -->
  <h5 class="mb-3">📚 Blogs by <%= profileUser.name %></h5>

  <% if (blogs.length === 0) { %>
    <div class="card p-3 text-center">
      <p class="mb-0">No blogs published yet.</p>
    </div>
  <% } %>

  <% blogs.forEach(b => { %>
    <div class="card p-3 mb-3">
      <a href="/blog/<%= b.slug %>" class="fw-bold text-decoration-none">
        <%= b.title %>
      </a>
      <div class="text-muted small mt-1">
        🕒 <%= b.readingTime || 1 %> min read · 👁️ <%= b.views || 0 %> views
      </div>
    </div>
  <% }) %>

</div>

</body>
</html>
