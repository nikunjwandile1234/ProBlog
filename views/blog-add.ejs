<!DOCTYPE html>
<html>
<head>
  <%- include('./partials/head') %>
  <title>Write Blog</title>
</head>
<body>

<%- include('./partials/nav') %>

<div class="container mt-5 mb-5" style="max-width:800px">

  <div class="card p-4">

    <h3 class="mb-3 text-center">Write a New Blog ✍️</h3>

    <p class="text-muted text-center mb-4">
      ⚠️ Image must be less than <b>1MB</b> for mobile uploads.
    </p>

    <% if (typeof error !== "undefined") { %>
      <div class="alert alert-danger"><%= error %></div>
    <% } %>

    <form method="POST" action="/blog" enctype="multipart/form-data">

      <input 
        name="title" 
        class="form-control mb-3" 
        placeholder="Blog Title" 
        required
      >

      <input 
        name="category" 
        class="form-control mb-3" 
        placeholder="Category (e.g. AI, Web, Life)"
      >

      <input 
        name="tags" 
        class="form-control mb-3" 
        placeholder="Tags (comma separated)"
      >

      <label class="mb-1">Cover Image (Max 1MB)</label>
      <input 
        type="file" 
        name="cover" 
        class="form-control mb-3" 
        accept="image/*"
        required
      >

      <!-- CONTENT -->
      <label class="mb-1">Blog Content</label>
      <textarea 
        name="content" 
        class="form-control mb-3" 
        rows="12"
        placeholder="Write or paste your blog content here..."
        required
        autocorrect="on"
        autocapitalize="on"
        spellcheck="true"
      ></textarea>

      <button class="btn btn-primary w-100 btn-lg">
        🚀 Publish Blog
      </button>

    </form>

  </div>

</div>

</body>
</html>

